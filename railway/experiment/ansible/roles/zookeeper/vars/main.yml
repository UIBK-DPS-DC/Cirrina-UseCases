zookeeper_my_id: "{{ ansible_play_hosts.index(inventory_hostname) + 1 }}"
zookeeper_servers: "{% for host in groups['zookeeper_servers'] %}server.{{ loop.index }}={{ hostvars[host]['ansible_host'] }}:2888:3888;2181{% if not loop.last %} {% endif %}{% endfor %}"
